import React from "react";

const Cart = ({ cart, setCart }) => {
  console.log(cart);
  return (
    <div style={{ width: "40%" }}>
      <h1>Shopping cart</h1>
    </div>
  );
};

export default Cart;
